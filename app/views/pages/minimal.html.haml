.url
  = form_with url: @page, local: true do |form|
    = render "page_url"
    = form.submit "Find", :name => "Find"
    = form.submit "Refetch", :name => "Refetch"
    = form.submit "Store", :name => "Store"
