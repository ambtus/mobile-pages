- content_for :title do
  = "Page create error"
.form
  = form_for @page.becomes(Page) do |f|
    = render :partial => "pages/create", :locals => {:page => @page, :called_by => "error"}
    = f.submit "Find", :name => "Find"
    = f.submit "Store", :name => "Store"
