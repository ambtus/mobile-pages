-title = "Edit Text #{@section_number} for page: #{@page.title}"
-content_for(:title, title)
%h1=title
.content
  = form_for @page do |f|
    = f.submit "Preview Text"
    = hidden_field_tag(:section, @section_number)
    = text_area_tag :edited, @section, :rows => 30, :cols => 33
