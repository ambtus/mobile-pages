- content_for :title do
  = @page.title
= render :partial => "pages/buttons", :locals => {:page => @page }
= render :partial => "pages/links", :locals => {:page => @page, :called_by => "single" }
= render :partial => "pages/headers", :locals => {:page => @page, :called_by => "single" }
.content= render :text => @page.original_html
