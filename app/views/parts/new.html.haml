.form
  = form_with model: @page.becomes(Page), local: true do |form|
    = render "multiple_urls"
    .soon= render "pages/page_soon"
    = render "tags/update"
    = render "pages/page_fields"
    = form.submit "Store", :name => "Store"
