- content_for :title do
  = "New authors for " + @page.title

.title= @page.title
.authors
  - form_tag authors_path do
    = hidden_field_tag(:page_id, @page.id)
    = onblur_text_tag :authors, Author::NEW_PLACEHOLDER, :maxlength => 255, :size => 70
    .button= submit_tag "Add Authors"
