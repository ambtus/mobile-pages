-title="Edit raw html for #{@page.title}"
-content_for(:title, title)
%h1=title
.content
  = form_for @page do |f|
    .button= f.submit "Update Raw HTML"
    = text_area_tag :pasted, @page.raw_html, :rows => 10, :cols => 80
